import React from "react";
import './ChildList.css';

export const ChildList = ({CaseManagement, Name, Sex, DOB, Status}) => {
    const handleClick = (id) => event => {
        return null;
    }

    return (
        <div className="itemcontainer">

        </div>
    )
}